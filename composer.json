{
	"name": "brainsware/bacon-dist",
	"description": "Distribution package for bacon",
	"version": "dev-master",
	"license": "Apache-2.0",

	"repositories": [
		{ "type": "composer", "url": "http://local.satis" }
	],

	"require": {
		"brainsware/bacon": "dev-master"
	},

	"scripts": {
		"post-install-cmd": "touch test"
	}
}
